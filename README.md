LV: Dependency for KiU-Net (PyTorch) BRATS experiment. Patched to work with more up-to-date PyTorch versions.

### How to install

```
git clone https://github.com/thuyen/multicrop.git

cd multicrop

python setup.py install
```
